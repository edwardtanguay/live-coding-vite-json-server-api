{
  "name": "live-coding-vite-json-server-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "api": "json-server --watch ./data/data.json --port 5011",
    "start": "run-p api dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite": "^2.9.0"
  },
  "dependencies": {
    "json-server": "^0.17.0",
    "node-fetch": "^3.2.3"
  }
}
